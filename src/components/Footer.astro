---
import { GitHubIcon, LinkedInIcon, EmailIcon, XIcon } from './Icons';
import { socialLinks, socialIconClass } from '@/config/socialLinks';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
const currentYear = new Date().getFullYear();
---

<footer class={`mt-12 lg:mt-16 ${className || ''}`}>
  <div class="max-w-[900px] mx-auto px-4 lg:px-0">
    <div class="border-t border-border py-8 lg:py-12">
      {/* Copyright and Social Links - Flex Layout */}
      <div class="flex flex-col lg:flex-row items-center justify-between gap-4 lg:gap-6">
        {/* Copyright */}
        <p class="text-[0.75rem] lg:text-[0.8125rem] text-text-muted order-2 lg:order-1 text-center lg:text-left">
          © {currentYear} Alban Istrefi. Content{' '}
          <a
            href="https://creativecommons.org/licenses/by/4.0/"
            target="_blank"
            rel="noopener noreferrer"
            class="hover:text-accent-primary transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-accent-primary focus:ring-offset-2 focus:ring-offset-background-primary rounded underline decoration-1 underline-offset-2"
          >
            CC BY-4.0
          </a>.
        </p>

        {/* Social Links */}
        <div class="flex items-center gap-4 lg:gap-6 order-1 lg:order-2">
          <a
            href={socialLinks.github}
            target="_blank"
            rel="noopener noreferrer"
            class={socialIconClass}
            aria-label="Visit GitHub profile"
          >
            <GitHubIcon size="1.125rem" />
          </a>
          <a
            href={socialLinks.linkedin}
            target="_blank"
            rel="noopener noreferrer"
            class={socialIconClass}
            aria-label="Visit LinkedIn profile"
          >
            <LinkedInIcon size="1.125rem" />
          </a>
          <a
            href={socialLinks.x}
            target="_blank"
            rel="noopener noreferrer"
            class={socialIconClass}
            aria-label="Visit X profile"
          >
            <XIcon size="1.125rem" />
          </a>
          <a
            href={socialLinks.email}
            class={socialIconClass}
            aria-label="Send email"
          >
            <EmailIcon size="1.125rem" />
          </a>
        </div>
      </div>

      
    </div>
  </div>
</footer>
